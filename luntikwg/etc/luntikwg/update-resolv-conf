#!/usr/bin/bash

case $1 in
up)
  #DNS leak off
  echo -e "# Generated by LuntikWG\nnameserver 1.1.1.1\nnameserver 1.0.0.1" > /etc/resolv.conf
  ;;
down)
  #DNS restore
  pgrep NetworkManager && systemctl restart NetworkManager.service || resolvconf -u
  ;;
esac

exit 0
